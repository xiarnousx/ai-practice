services:
  cli:
    build:
      context: .
      dockerfile: docker/Dockerfile
    volumes:
      - .:/app
    working_dir: /app
    env_file:
      - .env
    container_name: python_cli_dev
    tty: true